'use client';

import React from 'react';

import {Box} from '@mui/material';

const ProfileBase = () => {
  return (
    <div>
      <Box>Profile</Box>
    </div>
  );
};

export default ProfileBase;
